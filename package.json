{
  "name": "ai-researcher",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web -c magenta,cyan \"python -m backend.main\" \"npm run dev --prefix frontend\"",
    "dev:web": "npm run dev --prefix frontend",
    "dev:api": "python -m backend.main"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
